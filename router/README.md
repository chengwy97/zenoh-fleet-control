# router

Zenoh router configuration, TLS/mTLS examples, ACL rules, and local development setup.

Use `scripts/verify-zenoh-tls-mtls.sh` to generate temporary certificates and verify the router accepts mTLS connections and rejects unauthenticated TLS clients.
